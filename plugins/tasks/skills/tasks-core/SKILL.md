---
name: tasks-core
description: "How to work with taskmgr: its command surface, and the conventions of the store being written to."
when_to_use: "Use before any work on a task, issue or ticket — filing one, finding what to work on, writing or repairing a body, recording progress, closing. Triggers on 'taskmgr', 'file a task', 'what should I work on', 'close the issue', 'the backlog'. Also loaded by name when another skill needs the tracker."
allowed-tools: Bash(taskmgr *)
---

# taskmgr

!`taskmgr guide 2>/dev/null || echo "STOP: taskmgr is not on PATH, so nothing above was printed. Read references/troubleshooting.md in this skill's directory, follow it, and file nothing until it resolves."`
