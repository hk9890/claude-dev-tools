# Resolving references

Fixture for Check E. Both halves are locally owned and both targets exist, so this file must pass.

A skill: load `tasks:tasks-core` before any tracker work.

An agent, which lives under `agents/` rather than `skills/`: `project-review:project-reviewer`.
