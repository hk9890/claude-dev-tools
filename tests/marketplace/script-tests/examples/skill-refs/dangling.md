# Dangling skill reference

Fixture for Check E. `tasks` is a real plugin directory in this repo, so the prefix is owned
locally and the reference must resolve — but no `tasks-nonexistent` skill exists.

Load `tasks:tasks-nonexistent` before filing anything.
